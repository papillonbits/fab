export const placeholderURL = Object.freeze('https://via.placeholder.com/300')

export const keyCode = Object.freeze({
  enter: 13,
})

export const defaultBreed = Object.freeze({
  dog: 'collie',
  cat: 'beng',
  unknown: 'unknown',
})

export const httpResponseCode = Object.freeze({
  success: 200,
  notFound: 404,
})
